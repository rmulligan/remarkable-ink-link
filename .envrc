layout python

# Automatically activate poetry's venv if present
if [ -f pyproject.toml ]; then
  export VIRTUAL_ENV=$(poetry env info --path 2>/dev/null)
  if [ -n "$VIRTUAL_ENV" ] && [ -d "$VIRTUAL_ENV" ]; then
    export PATH="$VIRTUAL_ENV/bin:$PATH"
  fi
fi
